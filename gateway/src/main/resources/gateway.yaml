services:
  - name: "TestService"
    url: "http://localhost:8081"
    essential: false
    insightUri: "/api"
routes:
  - from: "TestService"
    to: "TestService"
persistence:
  redis:
    url: "localhost:6379"
  db:
    url: "localhost:5432"
    username: admin
    password: admin
    type: PostgreSql